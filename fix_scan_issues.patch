--- a/pixelprobe/services/scan_service.py
+++ b/pixelprobe/services/scan_service.py
@@ -7,6 +7,7 @@ import json
 import threading
 import logging
 from datetime import datetime, timezone
+import time
 from typing import List, Dict, Optional, Tuple
 
 from flask import current_app
@@ -479,7 +480,9 @@ class ScanService:
                         # Create chunks for each directory
                         chunks = []
                         for dir_path, files in files_by_dir.items():
-                            chunk_id = hashlib.md5(f"{scan_state.scan_id}:{dir_path}".encode()).hexdigest()
+                            # Add timestamp to ensure uniqueness
+                            timestamp = time.time()
+                            chunk_id = hashlib.md5(f"{scan_state.scan_id}:{dir_path}:{timestamp}".encode()).hexdigest()
                             chunk = ScanChunk(
                                 scan_id=scan_state.scan_id,
                                 chunk_id=chunk_id,
@@ -501,7 +504,8 @@ class ScanService:
                         scan_state.phase_total = total_files
                         scan_state.total_chunks = len(chunks)
                         scan_state.start_time = datetime.now(timezone.utc)
-                        scan_state.progress_message = f'Scanning {total_files} selected files across {len(chunks)} directories...'
+                        # Truncate message to avoid VARCHAR limit
+                        scan_state.progress_message = f'Scanning {total_files} files in {len(chunks)} dirs'[:200]
                         db.session.commit()
                         
                         # For selected files, we need a special chunk processor
@@ -783,6 +787,24 @@ class ScanService:
                 if batch_counter % 10 == 0:
                     tracker.update_status(f"Processing batch {batch_counter} of {total_expected_batches}")
                     
+            except Exception as e:
+                logger.error(f"Error in scan batch processing: {e}")
+                # Try to recover
+                try:
+                    db.session.rollback()
+                    
+                    # Update scan state with error
+                    scan_state = db.session.get(ScanState, scan_state_id)
+                    if scan_state:
+                        scan_state.crash_count = (scan_state.crash_count or 0) + 1
+                        scan_state.last_crash_time = datetime.now(timezone.utc)
+                        scan_state.error_message = str(e)[:500]  # Truncate error message
+                        db.session.commit()
+                        
+                    # Continue with next batch if possible
+                    batch_start = batch_end
+                    continue
+                except:
+                    break
                     
         except Exception as e:
             logger.error(f"Error in sequential scan: {e}")
@@ -1418,7 +1440,9 @@ class ScanService:
         try:
             # Create a special chunk for pending files
             timestamp = datetime.now(timezone.utc).isoformat()
-            chunk_id = hashlib.md5(f"{scan_id}:PENDING_FILES:{timestamp}".encode()).hexdigest()
+            # Include microseconds for better uniqueness
+            unique_id = f"{scan_id}:PENDING_FILES:{timestamp}:{time.time()}"
+            chunk_id = hashlib.md5(unique_id.encode()).hexdigest()
             chunk = ScanChunk(
                 scan_id=scan_id,
                 chunk_id=chunk_id,
@@ -1449,7 +1473,9 @@ class ScanService:
             chunks_to_add = []
             
             for dir_path in directories_to_process:
-                chunk_id = hashlib.md5(f"{scan_id}:{dir_path}".encode()).hexdigest()
+                # Add timestamp for uniqueness
+                timestamp = time.time()
+                chunk_id = hashlib.md5(f"{scan_id}:{dir_path}:{timestamp}".encode()).hexdigest()
                 chunk = ScanChunk(
                     scan_id=scan_id,
                     chunk_id=chunk_id,

--- a/media_checker.py
+++ b/media_checker.py
@@ -1022,6 +1022,10 @@ class PixelProbe:
         
         try:
             file_size = os.path.getsize(file_path)
+            
+            # Maximum file size for processing (5GB)
+            MAX_FILE_SIZE = 5 * 1024 * 1024 * 1024
+            
             # Get file size and calculate appropriate timeout
             # Base timeout of 30 seconds + 1 second per 10MB
             timeout = 30 + (file_size // (10 * 1024 * 1024))
@@ -1029,6 +1033,13 @@ class PixelProbe:
             # Cap timeout at 5 minutes for very large files
             timeout = min(timeout, 300)
             
+            # Skip processing very large files that might hang
+            if file_size > MAX_FILE_SIZE:
+                logger.warning(f"Skipping very large file (>{MAX_FILE_SIZE/1024/1024/1024}GB): {file_path}")
+                scan_output.append(f"File too large for processing: {file_size/1024/1024/1024:.2f}GB")
+                has_warnings = True
+                warning_details.append(f"File exceeds maximum size limit of {MAX_FILE_SIZE/1024/1024/1024}GB")
+            else:
             # Run ffprobe with timeout
             result = subprocess.run(
                 ['ffprobe', '-v', 'error', '-show_format', '-show_streams', 
@@ -1102,6 +1113,16 @@ class PixelProbe:
                     scan_output.append(f"FFmpeg stderr:\n{result.stderr}")
                     
         except subprocess.TimeoutExpired:
+            # Mark as timeout in database
+            corruption_details.append(f"FFmpeg timeout after {timeout} seconds")
+            scan_output.append(f"FFmpeg validation: TIMEOUT after {timeout}s")
+            has_warnings = True
+            warning_details.append(f"Scan timeout for large file: {file_size/1024/1024:.2f}MB")
+            logger.warning(f"FFmpeg timeout for: {file_path} (size: {file_size/1024/1024:.2f}MB)")
+            
+            # Don't mark as corrupted, just warned
+            is_corrupted = False
+            
             corruption_details.append("FFmpeg timeout")
             scan_output.append("FFmpeg validation: TIMEOUT")
             logger.warning(f"FFmpeg timeout for: {file_path}")

--- a/models.py
+++ b/models.py
@@ -241,8 +241,8 @@ class ScanState(db.Model):
     total_files = db.Column(db.Integer, nullable=True, default=0)
     start_time = db.Column(db.DateTime, nullable=True)
     current_file = db.Column(db.String(500), nullable=True)
-    progress_message = db.Column(db.String(200), nullable=True)
-    error_message = db.Column(db.String(500), nullable=True)
+    progress_message = db.Column(db.String(1000), nullable=True)  # Increased from 200
+    error_message = db.Column(db.String(1000), nullable=True)  # Increased from 500
     
     # Chunk processing
     total_chunks = db.Column(db.Integer, nullable=True, default=0)
@@ -251,6 +251,10 @@ class ScanState(db.Model):
     # Cancel flag
     cancel_requested = db.Column(db.Boolean, nullable=False, default=False)
     
+    # Crash recovery tracking
+    crash_count = db.Column(db.Integer, nullable=False, default=0)
+    last_crash_time = db.Column(db.DateTime, nullable=True)
+    
     # Relationships
     chunks = db.relationship('ScanChunk', backref='scan', lazy='dynamic', cascade='all, delete-orphan')
     report = db.relationship('ScanReport', backref='scan', uselist=False, cascade='all, delete-orphan')
@@ -381,8 +385,8 @@ class CleanupState(db.Model):
     cleanup_id = db.Column(db.Integer, primary_key=True)
     is_active = db.Column(db.Boolean, nullable=False, default=False)
     current_file = db.Column(db.String(500), nullable=True)
-    progress_message = db.Column(db.String(200), nullable=True)
-    error_message = db.Column(db.String(500), nullable=True)
+    progress_message = db.Column(db.String(1000), nullable=True)  # Increased from 200
+    error_message = db.Column(db.String(1000), nullable=True)  # Increased from 500
     files_processed = db.Column(db.Integer, nullable=False, default=0)
     orphaned_files_found = db.Column(db.Integer, nullable=False, default=0)
     files_deleted = db.Column(db.Integer, nullable=False, default=0)
@@ -407,8 +411,8 @@ class FileChangesState(db.Model):
     total_files = db.Column(db.Integer, nullable=False, default=0)
     changes_found = db.Column(db.Integer, nullable=False, default=0)
     current_file = db.Column(db.String(500), nullable=True)
-    progress_message = db.Column(db.String(200), nullable=True)
-    error_message = db.Column(db.String(500), nullable=True)
+    progress_message = db.Column(db.String(1000), nullable=True)  # Increased from 200
+    error_message = db.Column(db.String(1000), nullable=True)  # Increased from 500
     
     # Phase tracking
     phase = db.Column(db.String(50), nullable=True)
@@ -426,7 +430,7 @@ class FileChangesState(db.Model):
 class ScanChunk(db.Model):
     __tablename__ = 'scan_chunks'
     id = db.Column(db.Integer, primary_key=True)
-    chunk_id = db.Column(db.String(100), nullable=False, unique=True, index=True)
+    chunk_id = db.Column(db.String(100), nullable=False, index=True)  # Removed unique constraint
     scan_id = db.Column(db.Integer, db.ForeignKey('scan_state.scan_id'), nullable=False, index=True)
     directory_path = db.Column(db.String(500), nullable=False)
     phase = db.Column(db.String(50), nullable=False, default='pending')
@@ -438,6 +442,9 @@ class ScanChunk(db.Model):
     error_message = db.Column(db.Text, nullable=True)
     created_at = db.Column(db.DateTime, nullable=False, default=lambda: datetime.now(timezone.utc))
     
+    # Add composite unique constraint
+    __table_args__ = (db.UniqueConstraint('scan_id', 'chunk_id', name='uq_scan_chunks_scan_chunk'),)
+    
     def to_dict(self):
         return {
             'id': self.id,