# PixelProbe Configuration

# Comma-separated list of directories to scan for media files
SCAN_PATHS=/path/to/your/media,/another/path/to/media

# Secret key for Flask sessions (CHANGE THIS IN PRODUCTION!)
SECRET_KEY=your-very-secret-key-here

# Database configuration
DATABASE_URL=sqlite:///media_checker.db

# Optional: Limit the number of files to scan for testing (0 = unlimited)
MAX_FILES_TO_SCAN=0

# Optional: Number of parallel scanning workers (default: 4)
MAX_SCAN_WORKERS=4

# Optional: Timezone for timestamps (default: UTC)
TZ=UTC

# Optional: Set to development for debugging
FLASK_ENV=production